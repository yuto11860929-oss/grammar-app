import React from 'react';

export const TeacherAnalytics: React.FC = () => {
    return (
        <div>
            <h1>Student Analytics</h1>
            <p style={{ color: 'var(--color-text-muted)' }}>View student progress here.</p>
        </div>
    );
};
